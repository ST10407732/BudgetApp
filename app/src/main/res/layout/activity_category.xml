<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#000000">

    <!-- Add some margin to push the EditText further down -->
    <TextView
        android:id="@+id/tvBudgetTracker"
        android:text="BudgetTracker"
        android:textColor="#FFA500"
        android:textSize="24sp"
        android:layout_marginTop="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <!-- Added marginTop to push the EditText further below -->
    <EditText
        android:id="@+id/etCategoryName"
        android:hint="Enter category name"
        android:textColor="#FFFFFF"
        android:textColorHint="#FFA500"
        android:backgroundTint="#FFA500"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
    android:padding="16dp" />

    <Button
        android:id="@+id/btnAddCategory"
        android:text="Add Category"
        android:textColor="#000000"
        android:background="#FFA500"
        android:layout_marginTop="12dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/tvCategoryListTitle"
        android:text="Your Categories"
        android:textColor="#FFA500"
        android:textSize="18sp"
        android:layout_marginTop="24dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <ListView
        android:id="@+id/lvCategories"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"
        android:background="#111111"
        android:paddingTop="8dp"
        android:paddingBottom="8dp" />
</LinearLayout>
